This directory is for 3rd party libraries. 

It should be considered a read only kind of directory. Meaning, no editable 
application code should go in here only prepackaged, stand alone, versioned
libraries.